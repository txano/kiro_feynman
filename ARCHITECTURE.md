# System Architecture

## Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     Story Device (ESP32-S3)                  │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │                    Main Application                     │ │
│  │                      (main.cpp)                         │ │
│  └─────────┬──────────────────────────────┬────────────────┘ │
│            │                               │                  │
│  ┌─────────▼──────────┐        ┌──────────▼──────────────┐  │
│  │  WiFi Provisioning │        │     LED Matrix Control   │  │
│  │  (wifi_prov.cpp)   │        │    (led_matrix.cpp)      │  │
│  └─────────┬──────────┘        └──────────┬───────────────┘  │
│            │                               │                  │
│  ┌─────────▼──────────┐        ┌──────────▼───────────────┐ │
│  │   ESP-IDF WiFi     │        │  Adafruit NeoMatrix      │ │
│  │   + BLE Stack      │        │  (WS2812 Driver)         │ │
│  └─────────┬──────────┘        └──────────┬───────────────┘ │
│            │                               │                  │
└────────────┼───────────────────────────────┼──────────────────┘
             │                               │
             │                               │
        ┌────▼────┐                    ┌─────▼──────┐
        │  WiFi   │                    │  8x8 LED   │
        │ Network │                    │   Matrix   │
        └─────────┘                    └────────────┘
```

## Phase 1: Current Implementation

### Component Interaction Flow

```
Power On
   │
   ▼
Initialize Serial
   │
   ▼
Initialize LED Matrix ──────► Show "INIT" text
   │
   ▼
Initialize WiFi Provisioning
   │
   ▼
Check if Provisioned?
   │
   ├─ NO ──► Start SoftAP Provisioning ──► Show "No WiFi" (Orange)
   │            │
   │            ▼
   │         Create AP: STORY_XXXXXX
   │            │
   │            ▼
   │         Wait for Phone Connection
   │            │
   │            ▼
   │         Receive WiFi Credentials ──► Show "Connecting..." (Cyan)
   │            │
   │            ▼
   │         Connect to WiFi
   │            │
   │            └──────────────────────┐
   │                                   │
   └─ YES ─► Auto-connect to WiFi ────┤
                                       │
                                       ▼
                              WiFi Connected ──► Get IP from ESP-IDF
                                       │
                                       ▼
                              Show "IP: xxx.xxx.xxx.xxx" (Green, scrolling)
                                       │
                                       ▼
                                  Main Loop
                                  (continuously scroll IP)
```

### Event-Driven Architecture

```
┌──────────────────┐
│  WiFi/AP Events  │
└────────┬─────────┘
         │
         ▼
┌────────────────────────────────────────┐
│      Event Handler                     │
│  (wifi_provisioning.cpp)               │
│                                        │
│  • WIFI_PROV_START                     │
│  • WIFI_PROV_CRED_RECV                 │
│  • WIFI_PROV_CRED_SUCCESS              │
│  • WIFI_PROV_CRED_FAIL                 │
│  • WIFI_EVENT_STA_DISCONNECTED         │
│  • WIFI_EVENT_AP_STACONNECTED          │
│  • IP_EVENT_STA_GOT_IP                 │
└────────┬───────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│   Status Callback                      │
│   (wifi_status_callback)               │
│   Updates: wifi_connected flag         │
│            ip_display_text             │
└────────┬───────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│   Main Loop                            │
│   • Gets IP from ESP-IDF netif         │
│   • Updates display text               │
│   • Scrolls text on LED matrix         │
└────────────────────────────────────────┘
```

## Phase 2: Audio Streaming (Planned)

### Extended Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     Story Device (ESP32-S3)                  │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │                    Main Application                     │ │
│  └─────┬──────────┬──────────────┬──────────────┬─────────┘ │
│        │          │              │              │            │
│  ┌─────▼────┐ ┌──▼──────┐ ┌─────▼─────┐ ┌──────▼────────┐  │
│  │   WiFi   │ │   LED   │ │   Audio   │ │   Storage     │  │
│  │   Prov   │ │  Matrix │ │  Streaming│ │   (SPIFFS)    │  │
│  └──────────┘ └─────────┘ └─────┬─────┘ └───────────────┘  │
│                                  │                           │
│                            ┌─────▼─────┐                     │
│                            │ I2S Driver│                     │
│                            └─────┬─────┘                     │
└──────────────────────────────────┼───────────────────────────┘
                                   │
                             ┌─────▼──────┐
                             │  Amplifier │
                             │  + Speaker │
                             └────────────┘
```

### Audio Streaming Flow

```
User Selects Story
   │
   ▼
Request Audio Stream URL
   │
   ▼
HTTP Client Connects ──────► Show "Loading" on LED
   │
   ▼
Start Receiving Audio Data
   │
   ▼
Decode Audio (MP3/AAC)
   │
   ▼
Fill I2S Buffer
   │
   ▼
I2S DMA Transfer ──────────► Audio Output
   │                              │
   │                              ▼
   │                         Amplifier
   │                              │
   │                              ▼
   │                           Speaker
   │
   ▼
Show "Playing" Animation ───► LED Matrix
   │
   ▼
Monitor Playback
   │
   ├─ Buffer Low ──► Request More Data
   │
   ├─ Button Press ──► Pause/Resume
   │
   └─ End of Stream ──► Show "Complete"
```

## Phase 3: Full System (Future)

### Complete Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     Story Device (ESP32-S3)                  │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │                    Main Application                     │ │
│  │                                                          │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐             │ │
│  │  │   WiFi   │  │   Audio  │  │   UI     │             │ │
│  │  │  Manager │  │  Player  │  │ Manager  │             │ │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘             │ │
│  └───────┼─────────────┼─────────────┼────────────────────┘ │
│          │             │             │                       │
│  ┌───────▼──────┐ ┌────▼─────┐ ┌────▼──────┐               │
│  │ HTTP Client  │ │ I2S Audio│ │  Buttons  │               │
│  │   (Cloud)    │ │  Output  │ │   + ADC   │               │
│  └──────────────┘ └──────────┘ └───────────┘               │
│                                                              │
│  ┌──────────────┐ ┌────────────┐ ┌───────────┐             │
│  │ LED Matrix   │ │  Storage   │ │    OTA    │             │
│  │   Display    │ │  (SPIFFS)  │ │  Updates  │             │
│  └──────────────┘ └────────────┘ └───────────┘             │
└─────────────────────────────────────────────────────────────┘
                          │
                          │
                ┌─────────▼──────────┐
                │   Cloud Service    │
                │                    │
                │  • User Accounts   │
                │  • Story Catalog   │
                │  • Audio Streaming │
                │  • Personalization │
                └────────────────────┘
```

## Data Flow

### Provisioning Data Flow

```
Phone ──WiFi (SoftAP)──► ESP32 ──► NVS Storage
                           │
                           └──► WiFi Connection (STA mode)
                                  │
                                  ├──► Get IP from ESP-IDF
                                  │
                                  └──► Cloud Service (future)
```

### Audio Streaming Data Flow (Planned)

```
Cloud Service ──HTTP/HTTPS──► ESP32 ──I2S──► Amplifier ──► Speaker
                                │
                                └──Visual──► LED Matrix
```

### User Interaction Flow (Planned)

```
Buttons ──GPIO──► ESP32 ──► Story Selection
                    │
Potentiometer ──ADC──► Volume Control
                    │
                    └──Visual Feedback──► LED Matrix
```

## Memory Layout

### Flash Partitions

```
┌─────────────────────────────────────┐
│ 0x0000   Bootloader                 │
├─────────────────────────────────────┤
│ 0x8000   Partition Table            │
├─────────────────────────────────────┤
│ 0x9000   NVS (WiFi Credentials)     │
├─────────────────────────────────────┤
│ 0xF000   PHY Init Data              │
├─────────────────────────────────────┤
│ 0x10000  Application (2MB)          │
├─────────────────────────────────────┤
│          SPIFFS Storage (1MB)       │
│          (for cached stories)       │
└─────────────────────────────────────┘
```

### RAM Usage (Estimated)

```
┌─────────────────────────────────────┐
│ System (ESP-IDF + Arduino)  ~100KB  │
├─────────────────────────────────────┤
│ WiFi/BLE Stack              ~50KB   │
├─────────────────────────────────────┤
│ LED Matrix Buffers          ~2KB    │
├─────────────────────────────────────┤
│ Audio Buffers (future)      ~32KB   │
├─────────────────────────────────────┤
│ Application Code            ~20KB   │
├─────────────────────────────────────┤
│ Free for expansion          ~300KB  │
└─────────────────────────────────────┘
```

## Security Considerations

### Current (Phase 1)
- ✅ WPA2 WiFi encryption
- ✅ SoftAP provisioning with Security 1 + PoP
- ✅ Open AP (no password for provisioning network)
- ⚠️ Hardcoded PoP (should be unique per device in production)

### Future Phases
- 🔒 HTTPS for audio streaming
- 🔒 Device authentication tokens
- 🔒 Encrypted storage for sensitive data
- 🔒 Secure OTA updates

## Performance Targets

### Phase 1 (Current)
- Boot time: <3 seconds
- BLE provisioning: <30 seconds
- WiFi connection: <10 seconds

### Phase 2 (Audio)
- Audio latency: <500ms
- Buffer underruns: 0
- Streaming bitrate: 64-128 kbps

### Phase 3 (Full System)
- Button response: <100ms
- Story selection: <2 seconds
- Volume adjustment: Immediate
